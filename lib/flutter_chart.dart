import 'package:flutter/material.dart';

class Flutter_Charts extends StatefulWidget {
  const Flutter_Charts({Key? key}) : super(key: key);

  @override
  State<Flutter_Charts> createState() => _Flutter_ChartsState();
}

class _Flutter_ChartsState extends State<Flutter_Charts> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Charts"),
      ),

    );
  }
}
